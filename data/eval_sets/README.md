# Evaluation sets

Only small, synthetic, non-sensitive evaluation fixtures may be committed here.
Evaluation outputs and any department-derived datasets belong under
`$DEPTSLM_DATA_DIR` and must never be committed.
