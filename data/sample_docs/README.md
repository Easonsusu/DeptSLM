# Sample documents

Only small, synthetic, non-sensitive documents intended for tests or demos may
be committed here. Real department uploads belong in
`$DEPTSLM_DATA_DIR/uploads` and must never be copied into this repository.
