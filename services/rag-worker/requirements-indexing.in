# External dependencies for the indexing worker image. The local API and
# worker packages are installed separately with --no-deps.
-r ../../apps/api/requirements-worker.lock
huggingface-hub>=0.30.0,<1.0.0
pypdf>=6.14.2,<7.0.0
qdrant-client>=1.16.2,<1.17.0
