[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "deptslm-training-runtime"
version = "0.1.0"
description = "Private pinned offline Phase 14.2 LlamaFactory runtime"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.setuptools.packages.find]
include = ["deptslm_training_runtime*"]
